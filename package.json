{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm --filter main-app dev",
    "build": "pnpm --filter main-app build",
    "lint": "pnpm --filter main-app lint",
    "preview": "pnpm --filter main-app preview"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
